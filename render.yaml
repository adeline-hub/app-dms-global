services:
  - type: web
    name: dash-seasonal-template
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install chromium

    startCommand: gunicorn app:server

    envVars:
      - key: PYTHON_VERSION
        value: "3.10"